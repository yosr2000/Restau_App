import 'package:flutter/material.dart';

class CrepePage extends StatelessWidget {
  const CrepePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Menu'),

      ),
      body: Text("hi crepe"),
    );
  }
}
